[user]
    name = Hélder Silva
    email = <your.email@yourdomain.com>
[core]
    editor = vim
    fileMode = false
    bare = false
    logallrefupdates = true
[push]
    default = simple
[pull]
    rebase = true
[color]
    ui = auto
[merge]
    tool = meld
[color]
    ui = true
[alias]
    st = status
    ci = commit
    gl = log --graph --abbrev-commit --decorate --format=format:'%C(bold blue)%h%C(reset) - %C(bold cyan)%aD%C(reset) %C(bold green)(%ar)%C(reset)%C(bold yellow)%d%C(reset)%n''          %C(white)%s%C(reset) %C(dim white)- %an%C(reset)' --all
    jira = log -1 --no-merges --author=<your.email@yourdomain.com> --pretty --format="format:Commit *%H* on *%d*"
